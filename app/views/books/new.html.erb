 <h2>New book</h2>
      <%= form_with model: @book, url: '/books', method: :post do |f| %>
        <div>
          <label>Title</label>
          <%= f.text_field :title %>
        </div>
        <div>
          <ladel>Opinion</ladel>
          <%= f.text_area :body %>
        </div>
        <div>
          <%= f.submit 'Create Book' %>
        </div>
     <% end %>
